Welcome to my simple library project!

The code inside src consists of Main (which allows you to actually use the other classes), Book, Reader and Library.
A simple scenario has been created in Main which aims to simulate a user borrowing a book from the library.